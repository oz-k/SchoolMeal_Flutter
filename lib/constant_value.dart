import 'package:flutter/material.dart';

class ConstantValue {
    static bool isIcon = false;
    static const dates = [
        ['asset/moon.png', '월'], 
        ['asset/fire.png', '화'], 
        ['asset/water.png', '수'], 
        ['asset/tree.png', '목'], 
        ['asset/gold.png', '금']
    ];
    static const TextStyle titleTextStyle = TextStyle(fontSize: 20, color: Colors.black);
}